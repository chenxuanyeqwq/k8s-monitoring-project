# 集群内服务：Alertmanager 通过 http://feishu-bridge/alert 调用
apiVersion: v1
kind: Service
metadata:
  name: feishu-bridge
  namespace: monitoring
spec:
  selector:
    app: feishu-bridge
  ports:
    - port: 80
      targetPort: 8080
